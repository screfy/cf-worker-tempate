{
  "name": "cf-worker-template",
  "description": "A Cloudflare Worker template that uses TypeScript and a custom route parser.",
  "license": "MIT",
  "version": "0.1.0",
  "author": "screfy <screfy@pm.me> (https://screfy.com)",
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "dev": "miniflare -w",
    "deploy": "wrangler publish",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "route-parser": "^0.0.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.1",
    "@types/route-parser": "^0.1.4",
    "esbuild-loader": "^2.18.0",
    "fork-ts-checker-webpack-plugin": "^7.0.0",
    "miniflare": "^2.2.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5",
    "webpack": "^5.68.0",
    "webpack-cli": "^4.9.2"
  }
}
